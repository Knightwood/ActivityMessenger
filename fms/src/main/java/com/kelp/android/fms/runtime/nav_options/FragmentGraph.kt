package com.kelp.android.fms.runtime.nav_options

import com.kelp.android.fms.runtime.nav_options.NavOptions

/**
 * 在这里添加fragment的启动描述，作为深度导航所需的参数描述
 */
object FragmentGraph {
    val fragments = mutableListOf<NavOptions>()
}